<div class="container my-2">
  <ul class="list-group">
    {{#each users}}
      <a href="/users/{{this.id}}" class="list-group-item list-group-item-action">
        {{this.name}}
      </a>
    {{/each}}
  </ul>
</div>
